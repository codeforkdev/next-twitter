"use client";
import { user } from "@/mock-data";
import "./globals.css";
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import Link from "next/link";
import { cn } from "@/lib/utils";
import { usePathname } from "next/navigation";

const inter = Inter({ subsets: ["latin"] });

// export const metadata: Metadata = {
//   title: "Create Next App",
//   description: "Generated by create next app",
// };

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const pathname = usePathname();
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="flex flex-col sm:flex-row h-full w-full sm:mx-auto md:max-w-7xl">
          <div className="hidden sm:w-20 md:w-72 h-full border-x sm:flex flex-col">
            <Link href="/" className={cn({ "text-sky-500": pathname === "/" })}>
              Home
            </Link>
            <Link
              href="/messages"
              className={cn({ "text-sky-500": pathname === "/messages" })}
            >
              Messages
            </Link>
            <Link
              href="/bookmarks"
              className={cn({ "text-sky-500": pathname === "/bookmarks" })}
            >
              Bookmarks
            </Link>
            <Link
              href={"/" + user.handle}
              className={cn({ "text-sky-500": pathname === "/" + user.handle })}
            >
              Profile
            </Link>
          </div>
          <div className="flex-1 max-w-2xl border-r">{children}</div>
          <div className="h-32 border-t sm:hidden">Mobile Nav</div>
          {/* <div className="hidden lg:block lg:w-72 border-r">Aside Features</div> */}
        </div>
      </body>
    </html>
  );
}
